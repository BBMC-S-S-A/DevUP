// Mismo componente que `/app/o/[orgId]/buscar`, montado bajo la URL del
// workspace por el mismo motivo que el resto de pantallas de organización:
// ver el comentario de `NavegacionOrganizacion.tsx`.
export { default } from "@/app/(privado)/app/o/[orgId]/buscar/page";
